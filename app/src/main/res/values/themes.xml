<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.DarkActionBar.Bridge">
    <!--<style name="AppTheme" parent="Theme.GuidelinesCompat.Light.DarkToolbar.NoActionbar">-->
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/colorBackground</item>
        <item name="windowActionModeOverlay">true</item>

        <!-- Action Mode styles -->
        <item name="android:actionModeStyle">@style/AppTheme.ActionModeStyle</item>
        <item name="android:popupWindowStyle">@style/AppTheme.PopupWindowStyle</item>

        <!--<item name="actionModeBackground">@color/bg_action_mode</item>-->
    </style>

    <style name="AppTheme.ActionBar" parent="AppTheme">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:textColorSecondary">@color/grey</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="AppTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.ActionModeStyle" parent="AppTheme">
        <item name="background">@color/bg_action_mode</item>
    </style>

    <!-- Style taht controls how a popupWindow should look -->
    <style name="AppTheme.PopupWindowStyle" parent="AppTheme">
        <item name="android:popupBackground">@color/colorBackground</item>
        <item name="android:popupElevation">12dp</item>
    </style>

</resources>
